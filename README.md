# reports

Report writing with Quarto and markdown.

## Build reports locally

1. Install [Quarto](https://quarto.org/).
2. From the repository root, run:

   ```bash
   quarto render
   ```

The rendered site is written to `_site/`.
